#!/usr/bin/env python3
"""The written part of the test-session report.

Everything here is authored: the framing, the ordering, the priorities and the
argument for each. Verbatim material — the tester's per-run verdicts, the 18
patterns, the 115 catalogued errors — is pulled from their workbook so it is
quoted exactly rather than retyped.

The ordering is the one editorial decision that matters. The tester's sheets
are organised by when things were run; this is organised by what to fix first,
which is a different question and the one the team actually has.
"""

from __future__ import annotations

# --- what I concluded from reading the whole workbook -----------------------

LEDE = """120 прогонов за два дня, 32 по фотографии и 88 текстом. Тестировщик
разобрал вручную 115 ошибок бота и написал вердикт к каждому из 24 подробных
прогонов. Ниже — его материалы целиком, выстроенные по одному принципу: что
чинить первым."""

HEADLINE = """Бот считает математику правильно. Балл он ставит неправильно —
и в большинстве случаев из-за наших собственных правил, а не из-за модели."""

# The decisive number, and the one that reorders everything: 64 of 115 errors
# are attributed to the prompt, and they carry 24 of the 43 that cost points.
WHY_IT_MATTERS = """Из 115 разобранных ошибок <b>64 отнесены к «промпт /
правила пайплайна»</b> — это больше, чем к почерку (16), слабой модели (18) и
условиям съёмки (8) вместе. На балл влияют 43 ошибки, и 24 из них — снова
промпт. Это хорошая новость: самая массовая поломка чинится правкой текста в
<code>prompts/</code>, без смены модели и без денег."""

PRIORITIES = [
    {
        "n": 1,
        "title": "Убрать правила, из-за которых занижается оформление",
        "where": "prompts/stage3_grading.md",
        "cost": "правка текста, полдня",
        "why": """Самая массовая и самая дешёвая поломка. Три эталонных решения
        с «Решу ЕГЭ» из шести получили 1 балл за оформление вместо 2 — то есть
        бот снижает балл решению, которое сам сайт публикует как образцовое.
        В прогоне 20 бот <b>дословно процитировал правило нашего промпта</b>:
        «непрописанное обоснование и невыписанная ОДЗ — потеря, даже если шаг
        стандартный и на ответ не влияет». Источник занижения доказан, гадать
        не нужно.""",
        "evidence": """Прогоны 17, 18, 20 — эталоны занижены; 19, 21, 22 —
        эталоны с другим способом отбора приняты. Ключевой контраст: эталон с
        отбором двойными неравенствами → 2/2, эталон с отбором по окружности →
        1/2. Штрафуется именно способ, а не его отсутствие.""",
        "todo": [
            "убрать из промпта «потеря, даже если шаг стандартный» и «при сомнении — меньший балл»",
            "белый список допустимых обоснований: отбор по тригонометрической окружности с выделенной дугой, метод интервалов, перебор n, двойные неравенства",
            "находки с severity = cosmetic не штрафовать — правилом в коде, а не просьбой в промпте",
        ],
    },
    {
        "n": 2,
        "title": "Свериться с действующими критериями ФИПИ",
        "where": "prompts/criteria/13.md, 15.md",
        "cost": "сверка с документом, 1–2 дня",
        "why": """Критерии, зашитые в бота, расходятся с актуальными. Для №13
        нет варианта «1 балл за неверные ответы из-за вычислительной ошибки
        при верной последовательности всех шагов», зато есть «верный ответ
        только в б)». Для №15 бот даёт балл за включение лишних точек, а по
        «Решу ЕГЭ» балл полагается только за исключение. Это не промпт и не
        модель — это данные.""",
        "evidence": """Тест X1: бот <b>правильно</b> распознал вычислительную
        ошибку при верном ходе и правильно это описал, но поставил 0/0, потому
        что подходящего критерия у него нет. Ожидалось 1/1.""",
        "todo": [
            "решить, какая редакция критериев эталонная: ФИПИ или «Решу ЕГЭ» — сейчас они расходятся",
            "прогнать набор решений с одной вычислительной ошибкой при верном ходе",
        ],
    },
    {
        "n": 3,
        "title": "Сомнение распознавания — не штраф и не прощение, а ручная проверка",
        "where": "app/grading/pipeline.py — код, не промпт",
        "cost": "небольшая правка пайплайна",
        "why": """Этап 1 иногда сам помечает, что не уверен в прочитанном, — и
        этап 3 всё равно снимает балл. Дважды (прогоны 9 и 13) этап 2 прямо
        написал «если в рукописи другое — находка снимается», и всё равно
        балл сняли. А в прогоне P14 то же сомнение сработало наоборот: им
        <b>простили реальную ошибку ученика</b>. Правило в промпте не
        соблюдается ни в одну сторону, значит оно должно быть в коде.""",
        "evidence": """Прогоны 9, 13 — сомнение проигнорировано и балл снят;
        P14 — сомнение использовано, чтобы простить настоящую ошибку
        (завышение 2/1 вместо 1/1).""",
        "todo": [
            "находка с transcription_doubt и affects_answer → работа уходит на ручную проверку, балл не выставляется автоматически",
            "правило «грубая ошибка при безупречном остальном → сначала подозревать распознавание»",
            "сверять числа в ответе с подписями на чертеже: в прогоне 7 прочитанные корни даже не лежали на дуге, которую выделил ученик",
        ],
    },
    {
        "n": 4,
        "title": "Распознавание цифр: ошибки повторяемые, значит воспроизводимые в тесте",
        "where": "этап 1, vision-модель",
        "cost": "дорого — работа с моделью и препроцессингом",
        "why": """Главный риск для фото. Ошибки не случайные: ответ
        <code>{−13π/6; −17π/6; −19π/6}</code> прочитан как
        <code>{−13π/6; −11π/6; −7π/6}</code> <b>трижды на трёх разных
        снимках</b> одной работы, включая кадр во весь лист. Семёрка без
        перекладины читается как 1 или 3; исправленная 22 — как 23, после чего
        модель «согласует» под неё чёткие √22 дальше по тексту.""",
        "evidence": """Прогоны 7, 8, 9 — идентичная ошибка на 5.jpg, 6.jpg,
        7.jpg. Контроль: та же работа текстом (прогон 10) → 2/2. Балл терялся
        только из-за чтения цифр.""",
        "todo": [
            "несколько чтений на этапе 1 с голосованием по спорным символам",
            "автоповорот и обрезка по листу, коррекция яркости — 90° поворот уничтожил прогоны 3 и 13",
            "тест-набор на похожие цифры: 1/7, 3/7, 4/9, 5/6",
            "совет ученику в интерфейсе: лист во весь кадр, ровно, при белом свете",
        ],
    },
    {
        "n": 5,
        "title": "Убрать случайность из оценки",
        "where": "GRADING_SELF_CONSISTENCY, temperature",
        "cost": "настройка; стоит токенов",
        "why": """Одна и та же работа на разных фото получает 1/1, 2/1 и 0/0.
        На тексте повторы дают одинаковый результат — значит источник
        случайности — этап 1, а не оценивание. Но и в оценивании одна и та же
        находка «ОДЗ не выписана» в прогоне 20 наказана, а в прогоне 21
        прощена.""",
        "evidence": """Работа Г на трёх снимках: 8.jpg → 1/1, 9.jpg → 2/1,
        10.jpg (повёрнут) → 0/0. Работа Б, прогоны 3–6: оформление 2, 1, 2, 2.""",
        "todo": [
            "temperature 0 на этапах 2 и 3",
            "self_consistency ≥ 3 с голосованием по баллу — хотя бы для фото",
        ],
    },
    {
        "n": 6,
        "title": "Починить потерю отчёта при раннем выходе",
        "where": "app/grading/pipeline.py",
        "cost": "несколько строк",
        "why": """Подтверждённый баг нашего бэкенда: если этап 1 решил, что
        решения на фото нет, прогон завершается раньше — и отчёт не
        сохраняется. Тестировщик дважды не смог отправить отзыв: «нет
        отчёта… возможно, сервис перезапускали». То есть именно негативные
        случаи, ради которых и нужен отзыв, до нас не доходят.""",
        "evidence": """Колонка «Отзыв в боте» в листе «Логи»: два прогона с
        пометкой «НЕ ОТПРАВЛЕН … (баг)». Плюс уверенность 1.00 и 0.00 на двух
        одинаковых пустых фото.""",
        "todo": [
            "вызывать сохранение отчёта и на ветке is_solution_present = false",
            "фиксировать confidence при раннем выходе",
        ],
    },
]

WHAT_WORKS = """Собственное решение бота верно <b>22 раза из 22</b>. Базовая
оценка всех шести эталонов — 2/2. Из семи испорченных решений (X1–X7) бот
нашёл внесённую ошибку во всех семи. Негативные тесты — 6 из 6: мусорное фото
опознано как «решения нет» за две секунды, решение, отправленное под чужим
номером задания, получило 0 с явным указанием, что решена другая задача.
Ошибки в баллах — не от непонимания математики."""

MODE_STORY = """Разделение проходит не по заданиям и не по флагам, а по тому,
пришла работа фотографией или текстом. На тексте оценивание воспроизводимо и
ошибки систематические — их можно чинить. На фото балл зависит от того, какой
получился кадр."""

FLAGS_STORY = """Ни один флаг не устраняет основные проблемы. Выключение
«этап 2 сначала решает задачу сам» дало лучшую точность оформления на тексте —
77% против 54% — при тех же токенах, но это 13 тестов, то есть тенденция, а не
доказательство. Флаги этапа 1 распознавание не улучшают. Поле «Верный ответ»
сдвигает балл в обе стороны и безопасной подсказкой не является.
Гипотеза «если модель решает сама, она проверяет лояльнее» <b>не
подтвердилась</b>: завышение в тесте X7 сохраняется и с выключенным флагом —
лояльность вызывает совпадение ответа, а не собственное решение."""

COST_NOTE = """Стоимость сессии — около <b>$1</b> на 2,5 млн токенов. Почти
половина из них (49%) — «рассуждения» модели, за которые платится как за
вывод. Фотопрогон дороже текстового почти вдвое: 30 тыс. токенов против 17,5
тыс., 73 секунды против 48."""

CLOSING = """Вывод тестировщика: для текстового ввода бот близок к пригодности
после правки промпта оформления и критериев; по фотографии ставить балл без
ручной проверки нельзя. Приоритеты 1 и 2 выше закрывают текстовый сценарий и
стоят нескольких дней работы с текстом, без затрат на модель. Приоритет 4 —
отдельная задача другого масштаба."""
